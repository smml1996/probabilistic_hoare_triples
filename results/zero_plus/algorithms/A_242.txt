{
	x0 := measure(q0); 
	if c = 0:
		x0 := measure(q0); 
		if c = 0:
			HALT
		if c = 1:
			x0 := measure(q0); 
			if c = 0:
				HALT
			if c = 1:
				x0 := measure(q0); 
				if c = 0:
					HALT
				if c = 1:
					HALT
	if c = 1:
		x0 := measure(q0); 
		if c = 0:
			x0 := measure(q0); 
			if c = 0:
				HALT
			if c = 1:
				x0 := measure(q0); 
				if c = 0:
					HALT
				if c = 1:
					HALT
		if c = 1:
			x0 := measure(q0); 
			if c = 0:
				x0 := measure(q0); 
				if c = 0:
					HALT
				if c = 1:
					HALT
			if c = 1:
				HALT
} ⊕_0.705157 {
	x0 := measure(q0); 
	if c = 0:
		x0 := measure(q0); 
		if c = 0:
			x0 := measure(q0); 
			if c = 0:
				HALT
			if c = 1:
				x0 := measure(q0); 
				if c = 0:
					HALT
				if c = 1:
					HALT
		if c = 1:
			x0 := measure(q0); 
			if c = 0:
				x0 := measure(q0); 
				if c = 0:
					HALT
				if c = 1:
					HALT
			if c = 1:
				HALT
	if c = 1:
		x0 := measure(q0); 
		if c = 0:
			x0 := measure(q0); 
			if c = 0:
				x0 := measure(q0); 
				if c = 0:
					HALT
				if c = 1:
					HALT
			if c = 1:
				HALT
		if c = 1:
			HALT
}
