{
	H([q0]); 
	x0 := measure(q0); 
	if c in {0}:
		H([q0]); 
		x0 := !x0; 
		HALT
	if c in {1}:
		x0 := measure(q0); 
		x0 := !x0; 
		HALT
} ⊕_0.503727 {
	x0 := measure(q0); 
	if c in {0}:
		x0 := measure(q0); 
		if c in {0}:
			x0 := measure(q0); 
			if c in {0}:
				HALT
			if c in {1}:
				x0 := measure(q0); 
				HALT
		if c in {1}:
			x0 := measure(q0); 
			if c in {0}:
				x0 := measure(q0); 
				HALT
			if c in {1}:
				HALT
	if c in {1}:
		x0 := measure(q0); 
		if c in {0}:
			x0 := measure(q0); 
			if c in {0}:
				x0 := measure(q0); 
				HALT
			if c in {1}:
				HALT
		if c in {1}:
			HALT
}
