{
	x0 := measure(q0); 
	if c in {0}:
		H([q0]); 
		x0 := !x0; 
		HALT
	if c in {1}:
		x0 := measure(q0); 
		x0 := !x0; 
		HALT
} ⊕_0.867375 {
	x0 := measure(q0); 
	if c in {0}:
		H([q0]); 
		x0 := !x0; 
		HALT
	if c in {1}:
		H([q0]); 
		x0 := !x0; 
		HALT
}
