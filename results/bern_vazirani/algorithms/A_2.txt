{
	x0 := measure(q1); 
	if c in {0}:
		H([q1]); 
		x0 := !x0; 
		HALT
	if c in {1}:
		x0 := measure(q1); 
		if c in {0}:
			x0 := !x0; 
			HALT
		if c in {1}:
			x0 := measure(q1); 
			HALT
} ⊕_{0.504809} {
	x0 := measure(q1); 
	if c in {0}:
		H([q1]); CNOT([q0,q1]); H([q1]); 
		x0 := measure(q1); 
		HALT
	if c in {1}:
		H([q1]); 
		x0 := !x0; 
		HALT
}
