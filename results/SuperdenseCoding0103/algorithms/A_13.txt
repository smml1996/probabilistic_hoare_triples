{
	x0 := measure(q0); x1 := measure(q1); 
	if c in {0}:
		x0 := measure(q0); x1 := measure(q1); 
		if c in {0, 2}:
			x0 := !x0; x2 := 1; 
			HALT
		if c in {1}:
			x1 := !x1; x3 := 1; 
			HALT
		if c in {3}:
			HALT
	if c in {1}:
		X([q0]); 
		x1 := !x1; x3 := 1; 
		HALT
	if c in {2}:
		X([q0]); 
		x0 := !x0; x2 := 1; 
		HALT
	if c in {3}:
		x0 := measure(q0); x1 := measure(q1); 
		if c in {0, 2}:
			x0 := !x0; x2 := 1; 
			HALT
		if c in {1, 3}:
			x1 := !x1; x3 := 1; 
			HALT
} ⊕_{0.500000} {
	x0 := measure(q0); x1 := measure(q1); 
	if c in {0}:
		X([q0]); 
		x0 := !x0; x2 := 1; 
		HALT
	if c in {1}:
		x0 := measure(q0); x1 := measure(q1); 
		if c in {0, 2}:
			x0 := !x0; x2 := 1; 
			HALT
		if c in {1, 3}:
			x1 := !x1; x3 := 1; 
			HALT
	if c in {2}:
		x0 := measure(q0); x1 := measure(q1); 
		if c in {0, 2}:
			x0 := !x0; x2 := 1; 
			HALT
		if c in {1}:
			HALT
		if c in {3}:
			x1 := !x1; x3 := 1; 
			HALT
	if c in {3}:
		X([q0]); 
		x1 := !x1; x3 := 1; 
		HALT
}
