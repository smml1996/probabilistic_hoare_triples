{
	X([q0]); 
	x0 := measure(q0); 
	if c in {0}:
		x0 := measure(q0); 
		if c in {0}:
			X([q0]); 
			HALT
		if c in {1}:
			HALT
	if c in {1}:
		HALT
} ⊕_0.965259 {
	x0 := measure(q0); 
	if c in {0}:
		x0 := measure(q0); 
		if c in {0}:
			X([q0]); 
			HALT
		if c in {1}:
			x0 := measure(q0); 
			if c in {0}:
				X([q0]); 
				HALT
			if c in {1}:
				HALT
	if c in {1}:
		x0 := measure(q0); 
		if c in {0}:
			x0 := measure(q0); 
			if c in {0}:
				X([q0]); 
				HALT
			if c in {1}:
				HALT
		if c in {1}:
			HALT
}
