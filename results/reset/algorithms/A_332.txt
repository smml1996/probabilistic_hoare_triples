{
	x0 := measure(q0); 
	if c in {0}:
		HALT
	if c in {1}:
		x0 := measure(q0); 
		if c in {0}:
			HALT
		if c in {1}:
			X([q0]); 
			HALT
} ⊕_0.504284 {
	x0 := measure(q0); 
	if c in {0}:
		x0 := measure(q0); 
		if c in {0}:
			HALT
		if c in {1}:
			X([q0]); 
			HALT
	if c in {1}:
		X([q0]); 
		HALT
}
