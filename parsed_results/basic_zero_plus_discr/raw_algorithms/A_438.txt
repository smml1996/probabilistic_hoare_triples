{
	H([q0]); 
	x0 := measure(q0); 
	if c = 0:
		H([q0]); 
		x0 := 1; 
		HALT
	if c = 1:
		x0 := measure(q0); 
		if c = 0:
			x0 := 1; 
			HALT
		if c = 1:
			x0 := 0; 
			HALT

} ⊕_0.671393 {
	x0 := measure(q0); 
	if c = 0:
		x0 := measure(q0); 
		if c = 0:
			x0 := measure(q0); 
			if c = 0:
				HALT
			if c = 1:
				x0 := measure(q0); 
				if c = 0:
					HALT
				if c = 1:
					HALT
		if c = 1:
			x0 := measure(q0); 
			if c = 0:
				x0 := measure(q0); 
				if c = 0:
					HALT
				if c = 1:
					HALT
			if c = 1:
				HALT
	if c = 1:
		x0 := measure(q0); 
		if c = 0:
			x0 := measure(q0); 
			if c = 0:
				x0 := measure(q0); 
				if c = 0:
					HALT
				if c = 1:
					HALT
			if c = 1:
				HALT
		if c = 1:
			HALT
}
