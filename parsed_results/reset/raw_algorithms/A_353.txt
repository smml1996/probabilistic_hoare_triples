{
	x0 := measure(q0); 
	if c = 0:
		x0 := measure(q0); 
		if c = 0:
			x0 := measure(q0); 
			if c = 0:
				HALT
			if c = 1:
				x0 := measure(q0); 
				if c = 0:
					HALT
				if c = 1:
					x0 := measure(q0); 
					if c = 0:
						HALT
					if c = 1:
						x0 := measure(q0); 
						if c = 0:
							HALT
						if c = 1:
							X([q0]); 
							HALT
		if c = 1:
			x0 := measure(q0); 
			if c = 0:
				x0 := measure(q0); 
				if c = 0:
					HALT
				if c = 1:
					x0 := measure(q0); 
					if c = 0:
						HALT
					if c = 1:
						x0 := measure(q0); 
						if c = 0:
							HALT
						if c = 1:
							X([q0]); 
							HALT
			if c = 1:
				X([q0]); 
				x0 := measure(q0); 
				if c = 0:
					HALT
				if c = 1:
					x0 := measure(q0); 
					if c = 0:
						HALT
					if c = 1:
						X([q0]); 
						HALT
	if c = 1:
		X([q0]); 
		x0 := measure(q0); 
		if c = 0:
			x0 := measure(q0); 
			if c = 0:
				HALT
			if c = 1:
				x0 := measure(q0); 
				if c = 0:
					HALT
				if c = 1:
					x0 := measure(q0); 
					if c = 0:
						HALT
					if c = 1:
						X([q0]); 
						HALT
		if c = 1:
			x0 := measure(q0); 
			if c = 0:
				x0 := measure(q0); 
				if c = 0:
					HALT
				if c = 1:
					x0 := measure(q0); 
					if c = 0:
						HALT
					if c = 1:
						X([q0]); 
						HALT
			if c = 1:
				x0 := measure(q0); 
				if c = 0:
					x0 := measure(q0); 
					if c = 0:
						HALT
					if c = 1:
						X([q0]); 
						HALT
				if c = 1:
					X([q0]); 
					HALT

} ⊕_0.201699 {
	x0 := measure(q0); 
	if c = 0:
		x0 := measure(q0); 
		if c = 0:
			x0 := measure(q0); 
			if c = 0:
				x0 := measure(q0); 
				if c = 0:
					HALT
				if c = 1:
					x0 := measure(q0); 
					if c = 0:
						HALT
					if c = 1:
						x0 := measure(q0); 
						if c = 0:
							HALT
						if c = 1:
							X([q0]); 
							HALT
			if c = 1:
				x0 := measure(q0); 
				if c = 0:
					x0 := measure(q0); 
					if c = 0:
						HALT
					if c = 1:
						x0 := measure(q0); 
						if c = 0:
							HALT
						if c = 1:
							X([q0]); 
							HALT
				if c = 1:
					x0 := measure(q0); 
					if c = 0:
						x0 := measure(q0); 
						if c = 0:
							HALT
						if c = 1:
							X([q0]); 
							HALT
					if c = 1:
						X([q0]); 
						HALT
		if c = 1:
			x0 := measure(q0); 
			if c = 0:
				x0 := measure(q0); 
				if c = 0:
					x0 := measure(q0); 
					if c = 0:
						HALT
					if c = 1:
						x0 := measure(q0); 
						if c = 0:
							HALT
						if c = 1:
							X([q0]); 
							HALT
				if c = 1:
					x0 := measure(q0); 
					if c = 0:
						x0 := measure(q0); 
						if c = 0:
							HALT
						if c = 1:
							X([q0]); 
							HALT
					if c = 1:
						X([q0]); 
						HALT
			if c = 1:
				X([q0]); 
				x0 := measure(q0); 
				if c = 0:
					HALT
				if c = 1:
					x0 := measure(q0); 
					if c = 0:
						HALT
					if c = 1:
						X([q0]); 
						HALT
	if c = 1:
		X([q0]); 
		x0 := measure(q0); 
		if c = 0:
			x0 := measure(q0); 
			if c = 0:
				HALT
			if c = 1:
				x0 := measure(q0); 
				if c = 0:
					HALT
				if c = 1:
					x0 := measure(q0); 
					if c = 0:
						HALT
					if c = 1:
						X([q0]); 
						HALT
		if c = 1:
			x0 := measure(q0); 
			if c = 0:
				x0 := measure(q0); 
				if c = 0:
					HALT
				if c = 1:
					x0 := measure(q0); 
					if c = 0:
						HALT
					if c = 1:
						X([q0]); 
						HALT
			if c = 1:
				x0 := measure(q0); 
				if c = 0:
					x0 := measure(q0); 
					if c = 0:
						HALT
					if c = 1:
						X([q0]); 
						HALT
				if c = 1:
					X([q0]); 
					HALT
}
